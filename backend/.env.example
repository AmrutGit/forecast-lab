# All settings have working defaults (see backend/app/core/config.py) —
# this file is only needed if you want to override them locally.

# JSON list of allowed CORS origins for the frontend dev server.
# FORECAST_LAB_API_CORS_ALLOW_ORIGINS=["http://localhost:5173"]

# Number of top attribute values returned per (region, category, attribute_type) group.
# FORECAST_LAB_API_TOP_K=5
